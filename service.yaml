apiVersion: v1
kind: Service
metadata:
  name: currency-service
  namespace: default
  labels:
    app: currency-service
spec:
  type: NodePort
  selector:
    app: currency-service
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8000
      nodePort: 30080
